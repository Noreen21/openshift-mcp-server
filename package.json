{
  "name": "openshift_mcp_server",
  "version": "1.1.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build needed for JavaScript'",
    "start": "node index.js",
    "start:with-config": "MCP_REMOTE_KUBECONFIG=${MCP_REMOTE_KUBECONFIG:-${KUBECONFIG:-~/.kube/config}} node index.js",
    "dev": "NODE_ENV=development DEBUG=* node index.js",
    "dev:with-config": "MCP_REMOTE_KUBECONFIG=${MCP_REMOTE_KUBECONFIG:-${KUBECONFIG:-~/.kube/config}} NODE_ENV=development DEBUG=* node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -f docker/Dockerfile -t openshift-mcp-server .",
    "docker:run": "docker run -v ~/.kube/config:/app/.kube/config openshift-mcp-server",
    "k8s:deploy": "kubectl apply -f kubernetes/",
    "k8s:undeploy": "kubectl delete -f kubernetes/",
    "k8s:logs": "kubectl logs -l app=openshift-mcp-server -f"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "OpenShift/Kubernetes cluster monitoring and management MCP server for AI Code Assistant. Generated by Cursor - AI-powered code editor.",
  "funding": {
    "type": "github",
    "url": "https://github.com/sponsors/nchhabra"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.3.0",
    "@modelcontextprotocol/sdk": "^1.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3",
    "@kubernetes/client-node": "^1.3.0"
  }
}
